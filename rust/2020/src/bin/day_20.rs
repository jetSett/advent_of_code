// Lost all my code, TODO

type Int = i64;

struct Tile {
    inside: Vec<char>,
    borders: [Vec<char>; 4],

    id: Int,
}

impl Tile {
    fn from_lines(lines: &[Vec<char>]) -> Self {
        todo!()
    }
}

fn main() {}
